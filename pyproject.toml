[project]
name = "FileExplorer"
version = "1.0.0-dev.0"
description = "Integrated file system viewer."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "freecad-stubs>=1.0.21",
    "pyside6>=6.10.1",
    "ruff>=0.14.10",
]

[tool.ruff]
line-length = 100
indent-width = 4